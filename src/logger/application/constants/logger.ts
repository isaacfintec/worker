const LOG_ENTRIES = 'loggEntries';
const LOG_JOBS = 'jobs';

export const TYPES = {
  LOG_ENTRIES,
  LOG_JOBS,
};

export const ALL_TYPES = Object.values(TYPES);

export default {
  ALL_TYPES,
  TYPES,
};
